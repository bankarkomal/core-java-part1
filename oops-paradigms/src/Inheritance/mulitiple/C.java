package Inheritance.mulitiple;
//Java doesn't support multiple inhreritance through extends keywords but it is allows through interfaec.

public class C extends A {
    @Override
    void disply() {
        super.disply();
    }
}
